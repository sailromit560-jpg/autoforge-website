document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="css/homepage.css"><link rel="stylesheet" href="css/final-fixes.css">');document.addEventListener('DOMContentLoaded',()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));if(!reduce){const so=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;e.target.querySelectorAll('[data-count]').forEach(n=>{let end=+n.dataset.count,start=performance.now();function tick(t){let v=Math.min(end,Math.round(end*Math.min(1,(t-start)/1000)));n.textContent=v.toLocaleString('en-IN');if(v<end)requestAnimationFrame(tick)}requestAnimationFrame(tick)});so.unobserve(e.target)}),{threshold:.5});document.querySelectorAll('.stats').forEach(x=>so.observe(x))}let c=document.querySelector('.compare');if(c){let set=e=>{let r=c.getBoundingClientRect(),x=(e.clientX-r.left)/r.width*100;c.style.setProperty('--pos',Math.max(4,Math.min(96,x))+'%')};c.onpointerdown=e=>{c.setPointerCapture(e.pointerId);set(e)};c.onpointermove=e=>e.buttons&&set(e)}let rail=document.querySelector('.review-rail');if(rail&&!reduce){let t=setInterval(()=>rail.scrollBy({left:rail.clientWidth,behavior:'smooth'}),3800);rail.onmouseenter=()=>clearInterval(t)}let f=document.querySelector('#newsletter');if(f)f.onsubmit=e=>{e.preventDefault();f.innerHTML='<b>Welcome to AutoForge — your 10% code is FORGE10.</b>'};/* DEMO ONLY. Newsletter signup is local-only. Connect to a real email service from a secure backend before production. */});
